一、文件结构
合成国家队/
├── environment.py      # 5×5 游戏环境
├── evals.py            # V0～V4 评估函数（新增）
├── expectimax.py       # 搜索框架（修改为接受 eval_func）
├── test_ai.py          # 单局演示
└── run_experiments.py  # 批量实验脚本（后续自己写）

二、